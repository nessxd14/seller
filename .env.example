VITE_SUPABASE_URL=https://zxoxougwgstrarwymlvd.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# scripts/bootstrap_clientes.mjs only (one-off local script, not used by the built app).
# Direct Postgres connection string — get the real one from the Supabase project's
# Database settings (Connection string), NOT the same thing as VITE_SUPABASE_URL above.
DATABASE_URL=postgresql://postgres:postgres@127.0.0.1:54322/postgres
SHOPIFY_CLIENT_ID=your-shopify-client-id
SHOPIFY_CLIENT_SECRET=your-shopify-client-secret
SHOPIFY_STORE_DOMAIN=your-store.myshopify.com

# Integración de saldo real POS <-> Hermes (api/hermes/*.ts). SIN prefijo VITE_ a
# propósito: estas dos solo las leen las funciones serverless de /api (process.env del
# lado del servidor de Vercel), nunca el bundle del navegador. En Vercel se cargan en
# seller -> Settings -> Environment Variables, HERMES_SERVICE_ROLE_KEY marcada
# "Sensitive". Nunca la anon key de Hermes acá — tiene que ser la service_role.
HERMES_URL=https://vlthlcbcgvsrwxqazvne.supabase.co
HERMES_SERVICE_ROLE_KEY=your-hermes-service-role-key

# Brief S-F: api/cron/sync-despachos.ts corre sin sesión de usuario (lo dispara Vercel
# Cron, no un cajero), así que necesita su propia credencial para leer/escribir
# pendiente_sync_hermes_despacho bypasseando RLS — VITE_SUPABASE_URL ya apunta al
# proyecto correcto de Cation, pero la anon key no alcanza acá. Igual que
# HERMES_SERVICE_ROLE_KEY: se carga en Vercel -> Settings -> Environment Variables,
# marcada "Sensitive". Nunca la anon key de Cation acá — tiene que ser la service_role.
CATION_SERVICE_ROLE_KEY=your-cation-service-role-key

# Opcional: Vercel la agrega sola al header Authorization de cada invocación de Cron
# cuando está configurada como env var — sin ella, /api/cron/sync-despachos queda
# accesible sin ese chequeo (ver comentario en el archivo). Configurarla en Vercel ->
# Settings -> Environment Variables si todavía no está.
CRON_SECRET=your-cron-secret
